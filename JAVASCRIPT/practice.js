//create an algorithm to count the number of vowels in a given string
vowels=prompt("type a sentence")
let vowels = typeof String
let input = vowels.match(["a","e","i","o","u"])

if (vowels = input){
    console.log(input.length)
}

